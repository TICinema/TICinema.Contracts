syntax = "proto3";

option csharp_namespace = "TICinema.Contracts.Protos.Identity";

package identity;

service AuthService {
	rpc SendOtp (SendOtpRequest) returns (SendOtpResponse);
}

message SendOtpRequest {
	string identifier = 1;
	string type = 2;
}

message SendOtpResponse {
	bool ok = 1;
}